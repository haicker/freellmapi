services:
  freellmapi:
    build: .
    container_name: freellmapi
    ports:
      - "3001:3001"
    volumes:
      - freellmapi-data:/app/server/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  freellmapi-data:
